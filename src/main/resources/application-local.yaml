spring:
  datasource:
    url: jdbc:h2:file:./data/highpassdb;MODE=MySQL;NON_KEYWORDS=VALUE;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
  flyway:
    enabled: false
  h2:
    console:
      enabled: true
app:
  frontend-url: http://localhost:3000
  secure-cookie: false
